# Voice QA screenshots (V1 + V2)

Fecha de ejecución: 2026-03-29 (UTC)

Estado en este entorno:
- No se pudieron generar capturas nuevas porque el contenedor no tiene Flutter SDK (`flutter: command not found`) ni un emulador/dispositivo disponible.
- Este directorio queda preparado para adjuntar evidencia visual cuando se ejecute la batería en un entorno local/CI con Flutter y dispositivo.

Checklist sugerido de capturas pendientes:
1. PTT iniciando grabación (estado `Grabando`).
2. Cancelación de grabación (retorno a `Listo`).
3. Error de permisos de micrófono.
4. Upload + processing (`Subiendo audio` / `Procesando respuesta`).
5. Respuesta textual renderizada + transcript.
6. Reproducción de audio en curso (`Reproduciendo`).
7. Cambio de perfil de voz y persistencia.
8. Autoplay ON/OFF con evidencia de comportamiento.
9. Replay manual de última respuesta.
10. Convivencia con chat de texto (mensaje enviado por texto + bloque de voz visible).
